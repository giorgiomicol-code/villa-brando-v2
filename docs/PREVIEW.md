# Villa Brando V2 Preview

La preview pubblica viene distribuita esclusivamente dalla branch `rebuild/bilingual-static-v1` tramite GitHub Actions.

Questa configurazione non modifica `main` e non modifica il dominio di produzione `www.villabrando.com`.
